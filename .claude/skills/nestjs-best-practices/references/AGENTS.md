# AGENTS.md — NestJS Best Practices

> Auto-generated by `pnpm build`. Do not edit directly — edit files in `references/rules/` and rebuild.

> Generated: 2026-04-06T19:32:06.589Z
> Total rules: 22

---

## Purpose

Use this file as a compact routing guide for NestJS work in this stack. Read the critical rules first, then open only the matching files in `rules/` for the task at hand.

## Stack

- NestJS 11
- TypeScript strict mode
- PostgreSQL
- Drizzle ORM + Drizzle Kit
- Better Auth + `@thallesp/nestjs-better-auth`
- `class-validator` + `class-transformer`
- `@nestjs/swagger`
- `pnpm`

## Always Enforce

- `auth-better-auth-setup`: Use Better Auth as the only app auth system and disable Nest body parsing in bootstrap. See `rules/auth-better-auth-setup.md`.
- `auth-public-routes`: Protect routes by default and mark public endpoints explicitly with `@AllowAnonymous()`. See `rules/auth-public-routes.md`.
- `drizzle-no-raw-rows-as-response`: Map Drizzle rows to response DTOs before returning them from controllers or services. See `rules/drizzle-no-raw-rows-as-response.md`.
- `drizzle-schema-definition`: Define tables with `pgTable()` and export Drizzle infer types from schema files. See `rules/drizzle-schema-definition.md`.
- `dto-validation`: Validate every input DTO through the global `ValidationPipe` with whitelist, forbid, and transform enabled. See `rules/dto-validation.md`.

## Task Routing

- Auth or session changes: read `rules/auth-*.md`.
- DTO, controller, or response-shape changes: read `rules/dto-*.md` and `rules/ctrl-*.md`.
- Database, schema, query, relation, or migration changes: read `rules/drizzle-*.md`.
- Config changes: read `rules/config-*.md`.
- Feature layout changes: read `rules/struct-*.md` and `rules/svc-*.md`.
- Error handling, API semantics, logging, or tests: read the matching `rules/err-*`, `rules/api-*`, `rules/obs-*`, or `rules/test-*` files.
- Open `examples/feature-module/` only when you need a concrete implementation shape.
- Open `examples/anti-patterns/` only when you need a counterexample or review baseline.

## Rule Index

### auth

- `auth-better-auth-setup` (CRITICAL): Use Better Auth as the only app auth system and disable Nest body parsing in bootstrap. File: `rules/auth-better-auth-setup.md`.
- `auth-public-routes` (CRITICAL): Protect routes by default and mark public endpoints explicitly with `@AllowAnonymous()`. File: `rules/auth-public-routes.md`.
- `auth-session-access` (HIGH): Read the current user through `@Session()` typed from the Better Auth config. File: `rules/auth-session-access.md`.

### drizzle

- `drizzle-no-raw-rows-as-response` (CRITICAL): Map Drizzle rows to response DTOs before returning them from controllers or services. File: `rules/drizzle-no-raw-rows-as-response.md`.
- `drizzle-schema-definition` (CRITICAL): Define tables with `pgTable()` and export Drizzle infer types from schema files. File: `rules/drizzle-schema-definition.md`.
- `drizzle-migrations` (HIGH): Use generated Drizzle migrations and avoid schema push workflows in production. File: `rules/drizzle-migrations.md`.
- `drizzle-queries-and-transactions` (HIGH): Use typed Drizzle queries directly in services and wrap multi-step writes in transactions. File: `rules/drizzle-queries-and-transactions.md`.
- `drizzle-relations` (HIGH): Define Drizzle relations explicitly so relational queries stay typed and consistent. File: `rules/drizzle-relations.md`.

### dto

- `dto-validation` (CRITICAL): Validate every input DTO through the global `ValidationPipe` with whitelist, forbid, and transform enabled. File: `rules/dto-validation.md`.
- `dto-swagger-properties` (HIGH): Add `@ApiProperty` metadata to every DTO field that appears in the API contract. File: `rules/dto-swagger-properties.md`.

### controllers

- `ctrl-swagger-decorators` (HIGH): Document every controller with Swagger decorators for tags, operations, and responses. File: `rules/ctrl-swagger-decorators.md`.
- `ctrl-thin-controllers` (HIGH): Keep controllers limited to transport concerns and delegate business logic to services. File: `rules/ctrl-thin-controllers.md`.

### services

- `svc-no-repository-ceremony` (HIGH): Query Drizzle directly from services instead of adding repository wrapper layers. File: `rules/svc-no-repository-ceremony.md`.

### structure

- `struct-feature-modules` (HIGH): Organize code by feature module with clear module, controller, service, and DTO boundaries. File: `rules/struct-feature-modules.md`.

### config

- `config-env-validation` (HIGH): Validate every environment variable at startup through `ConfigModule.forRoot({ validate })`. File: `rules/config-env-validation.md`.
- `config-no-process-env` (HIGH): Inject `ConfigService` instead of reading `process.env` inside application code. File: `rules/config-no-process-env.md`.

### errors

- `err-nestjs-exceptions` (HIGH): Throw NestJS HTTP exceptions instead of raw errors for request-facing failures. File: `rules/err-nestjs-exceptions.md`.

### testing

- `test-e2e-supertest` (MEDIUM-HIGH): Cover request flows with Supertest-based E2E tests against the Nest application. File: `rules/test-e2e-supertest.md`.
- `test-unit-services` (MEDIUM-HIGH): Unit test service behavior in isolation, especially branch logic and failure paths. File: `rules/test-unit-services.md`.

### api

- `api-rest-conventions` (MEDIUM-HIGH): Use predictable REST resource naming, HTTP verbs, status codes, and route structure. File: `rules/api-rest-conventions.md`.

### observability

- `obs-structured-logging` (MEDIUM): Use structured Nest logging instead of ad hoc console output. File: `rules/obs-structured-logging.md`.

### tooling

- `tool-conventional-commits` (MEDIUM): Use Conventional Commits for commit subjects and related git workflow conventions. File: `rules/tool-conventional-commits.md`.

## Maintenance

- Detailed guidance lives in `rules/`.
- `SKILL.md` should stay short and operational.
- `metadata.json` is the machine-readable rule inventory.
- Rebuild this file with `pnpm build` after changing rule metadata or summaries.